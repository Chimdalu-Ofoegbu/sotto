sdk-version: 3.5.1
name: sotto-test
version: 0.1.0
source: daml
# Test/Daml-Script package: depends on the model as a data-dependency so the model
# DAR itself stays free of daml-script (keeps the deployed package clean).
dependencies:
  - daml-prim
  - daml-stdlib
  - daml-script
data-dependencies:
  - ../.daml/dist/sotto-0.1.0.dar
